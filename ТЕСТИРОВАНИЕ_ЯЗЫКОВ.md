# Инструкция по тестированию двуязычной системы

## ✅ Что было исправлено

1. **Таджикский язык установлен по умолчанию** (вместо русского)
2. **Исправлена загрузка переводов** - теперь переводы правильно загружаются из файлов
3. **Улучшен механизм переключения языков**
4. **Все кеши очищены** для применения изменений

## 🧪 Как протестировать

### Тест 1: Проверка языка по умолчанию

1. **Откройте сайт** в браузере в режиме инкогнито (чтобы не было кеша)
2. Вы должны увидеть **таджикский язык** по умолчанию:
   - "ИФТУТ" - заголовок
   - "Email ё рақами телефон" - поле ввода
   - "Парол" - поле пароля
   - "Ворид шудан ба система" - кнопка входа

### Тест 2: Переключение на русский

1. **Кликните на переключатель языка** в правом верхнем углу (🇹🇯)
2. Выберите **Русский (🇷🇺)**
3. Страница перезагрузится с русским языком:
   - "Email или номер телефона"
   - "Пароль"
   - "Войти в систему"

### Тест 3: Сохранение выбора языка

1. **Выберите русский язык** через переключатель
2. **Закройте браузер**
3. **Откройте сайт снова**
4. Вы должны увидеть **русский язык** (сохраняется в localStorage)

### Тест 4: После входа в систему

1. **Войдите** в систему с любым пользователем
2. В верхней панели найдите **переключатель языка** (между поиском и уведомлениями)
3. **Переключите язык**
4. Все тексты интерфейса должны измениться
5. **Выйдите и войдите снова** - выбранный язык должен сохраниться

## 🔍 Что проверять после переключения языка

### Таджикский язык (по умолчанию):
- ✅ Хуш омадед (Добро пожаловать)
- ✅ Панели идоракунӣ (Панель управления)
- ✅ Курсҳо (Курсы)
- ✅ Ҷадвал (Расписание)
- ✅ Донишҷӯён (Студенты)
- ✅ Муаллимон (Преподаватели)

### Русский язык:
- ✅ Добро пожаловать
- ✅ Панель управления
- ✅ Курсы
- ✅ Расписание
- ✅ Студенты
- ✅ Преподаватели

## 🐛 Если язык не переключается

### Решение 1: Очистить кеш браузера
1. Откройте DevTools (F12)
2. Перейдите на вкладку Application/Хранилище
3. Очистите Local Storage и Session Storage
4. Обновите страницу (Ctrl+F5)

### Решение 2: Очистить кеш Laravel
```bash
php artisan config:clear
php artisan route:clear
php artisan cache:clear
php artisan view:clear
```

### Решение 3: Пересобрать фронтенд
```bash
npm run build
```

## 📝 Проверка в базе данных

Откройте таблицу `users` и проверьте поле `locale`:
- Должно быть `tg` по умолчанию для новых пользователей
- При смене языка значение должно обновляться

## 🎯 Где смотреть переводы

Все переводы находятся в:
```
lang/
├── ru/    # Русские переводы
└── tg/    # Таджикские переводы
```

## ⚡ Быстрая проверка через браузер

1. Откройте DevTools (F12)
2. В Console выполните:
```javascript
// Проверить текущий язык
console.log(window.location.search)

// Принудительно переключить язык
window.location.href = '/?lang=tg'  // Таджикский
window.location.href = '/?lang=ru'  // Русский
```

## ✨ Дополнительные проверки

### Проверка валидации форм:
1. Попробуйте войти с неправильным паролем
2. Сообщения об ошибках должны быть на выбранном языке

### Проверка сообщений успеха:
1. Смените пароль
2. Сообщение "Пароль успешно изменен!" должно быть на выбранном языке

## 📞 Если проблемы остались

Проверьте следующее:
1. Файлы переводов существуют в `lang/tg/` и `lang/ru/`
2. Кеш очищен (все 4 команды выше)
3. Фронтенд пересобран (`npm run build`)
4. Сервер перезапущен

---

**Теперь сайт должен работать с таджикским языком по умолчанию!** 🇹🇯

