# Инструкция по двуязычной системе ИФТУТ

## ✅ Что было сделано

Ваш сайт теперь **полностью двуязычный** и поддерживает:
- 🇷🇺 **Русский язык** (по умолчанию)
- 🇹🇯 **Таджикский язык**

## 🎯 Основные возможности

### 1. Автоматическая смена языка
- Пользователь может выбрать язык в любой момент
- Выбранный язык **сохраняется** для каждого пользователя
- При следующем входе сайт откроется на выбранном языке

### 2. Где находится переключатель языка?
- **На главной странице входа** (Login) - в правом верхнем углу
- **После входа в систему** - в верхней панели между поиском и уведомлениями

### 3. Что переведено?
✅ Все разделы интерфейса:
- Страница входа
- Дашборд (панель управления)
- Курсы
- Расписание
- Задания
- Оценки
- Тесты
- Студенты
- Преподаватели
- Настройки

✅ Все системные сообщения:
- Сообщения об успехе/ошибке
- Валидация форм
- Подсказки

## 📋 Файлы переводов

Все переводы находятся в папке `lang/`:

```
lang/
├── ru/              # Русские переводы
│   ├── auth.php
│   ├── validation.php
│   ├── messages.php
│   ├── dashboard.php
│   ├── navigation.php
│   ├── courses.php
│   ├── lessons.php
│   ├── tests.php
│   ├── grades.php
│   ├── students.php
│   ├── teachers.php
│   └── schedule.php
└── tg/              # Таджикские переводы
    ├── auth.php
    ├── validation.php
    ├── messages.php
    ├── dashboard.php
    ├── navigation.php
    ├── courses.php
    ├── lessons.php
    ├── tests.php
    ├── grades.php
    ├── students.php
    ├── teachers.php
    └── schedule.php
```

## 🔧 Как редактировать переводы?

### Пример 1: Изменить текст на странице входа

1. Откройте файл `lang/ru/auth.php` для русского языка
2. Найдите нужный ключ, например:
```php
'login_title' => 'ИФТУТ',
```
3. Измените текст:
```php
'login_title' => 'ИФТУТ - Система управления',
```
4. Повторите для таджикского языка в `lang/tg/auth.php`

### Пример 2: Добавить новый текст

1. Откройте нужный файл (например, `lang/ru/messages.php`)
2. Добавьте новый ключ:
```php
'my_new_text' => 'Мой новый текст',
```
3. Добавьте перевод в `lang/tg/messages.php`:
```php
'my_new_text' => 'Матни нави ман',
```

## 🚀 Как использовать переводы в коде?

### В PHP контроллерах:
```php
return back()->with('success', __('auth.password_changed'));
```

### В Vue компонентах:
```vue
<template>
  <h1>{{ __('dashboard.welcome') }}</h1>
</template>

<script setup>
import { useTranslations } from '@/composables/useTranslations'
const { __ } = useTranslations()
</script>
```

## 🛠️ Техническая информация

### База данных
- Добавлено поле `locale` в таблицу `users`
- Язык сохраняется для каждого пользователя отдельно

### Роуты
- `POST /locale/change` - смена языка
- `GET /locale/current` - получение текущего языка

### Middleware
- `SetLocale` - автоматически устанавливает язык для каждого запроса

## 📱 Адаптивность

Переключатель языка адаптируется под размер экрана:
- **Десктоп**: показывает флаг и название языка
- **Мобильные**: показывает только флаг (компактный режим)

## ✨ Что дальше?

Система готова к использованию! Вы можете:

1. **Протестировать** смену языка на сайте
2. **Отредактировать** тексты под свои нужды
3. **Добавить** новые переводы по мере необходимости

## 📞 Поддержка

Если нужно добавить новые разделы или изменить существующие переводы, используйте файлы в папке `lang/` как шаблон.

---

**Готово!** Ваш сайт теперь полностью двуязычный 🎉

